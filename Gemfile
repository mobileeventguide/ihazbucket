# A sample Gemfile
source "https://rubygems.org"

# STACK
gem 'rake'
gem 'sinatra'
gem 'rack-flash3'

# VIEW
gem 'haml'
gem 'padrino-helpers'
gem 'sass'

# DATA
gem 'aws-sdk', '~> 2.2'

group :test, :development do
  gem 'dotenv'
  gem 'rspec'
end

group :test do
  gem 'rack-test'
end
